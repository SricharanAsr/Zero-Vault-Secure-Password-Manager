{
    "version": 2,
    "buildCommand": "npm install && cd App/secure_password_demo/client && npm install && npm run build",
    "outputDirectory": "App/secure_password_demo/client/dist",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/index.ts"
        },
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}